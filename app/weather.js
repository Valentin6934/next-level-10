export function readiness(state,temp,humidity){const c=state.checkins[new Date().toISOString().slice(0,10)]||{};const red=+c.knee>=4||+c.fatigue>=8||+c.soreness>=8||(+c.sleep>0&&+c.sleep<6.5)||temp>=35||(temp>=32&&humidity>=60);const orange=!red&&( +c.knee>=2||+c.fatigue>=6||+c.soreness>=6||(+c.sleep>0&&+c.sleep<8)||temp>=30||(temp>=27&&humidity>=70));return{red,orange}}
export function advice(temp,humidity){if(temp>=35||(temp>=32&&humidity>=60))return{level:'red',title:'ROUGE',text:'Pas de séance intense dehors.'};if(temp>=30||(temp>=27&&humidity>=70))return{level:'orange',title:'PRUDENCE',text:'Avant 10 h ou après 20 h, pauses fréquentes.'};if(temp>=25)return{level:'orange',title:'CHAUD',text:'Évite le soleil direct et bois régulièrement.'};return{level:'green',title:'OK',text:'Conditions favorables.'}}
export async function currentWeather(){return new Promise((resolve,reject)=>{if(!navigator.geolocation)return reject(new Error('Géolocalisation indisponible'));navigator.geolocation.getCurrentPosition(async p=>{try{const r=await fetch(`https://api.open-meteo.com/v1/forecast?latitude=${p.coords.latitude}&longitude=${p.coords.longitude}&current=temperature_2m,relative_humidity_2m`);const j=await r.json();resolve({temp:Math.round(j.current.temperature_2m),humidity:Math.round(j.current.relative_humidity_2m)})}catch(e){reject(e)}},reject)})}
